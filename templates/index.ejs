<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <a href="/add">Add Social Network</a>

    <h2>Added Social Networks </h2>
    <% for(let i = 0; i < docs.length; i++) {%>
        <div id="<%= docs[i]._id%>">
            <form method="POST" action="/<%= docs[i]._id%>?_method=DELETE">
                <h3><a href="/<%= docs[i].name%>">Name: <%=docs[i].name%></a></h3>
                <div class="field sn">Social Network: <%=docs[i].sn%></div>
                <div class="field url">URL: <%=docs[i].url%></div>
                <div class="field clicks">Clicks: <%=docs[i].clicks%></div>
                <input type="text" name="<%= docs[i]._name%>" hidden>
                <a href="/edit/<%= docs[i]._id%>">Edit</a>
                <button type="submit">Delete</button>
            </form>
        </div>
    <% } %> 

</body>
</html>