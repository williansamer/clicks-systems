<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-uWxY/CJNBR+1zjPWmfnSnVxwRheevXITnMqoEIeG1LJrdI0GlVs/9cVSyPYXdcSF" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <title>Home</title>
</head>
<body>
    <div class="container p-3">

        <a href="/add">Add Social Network</a>

        <h2>Added Social Networks </h2>
        <% for(let i = 0; i < docs.length; i++) {%>
            <div class="card mt-3 mb-3 p-1" id="<%= docs[i]._id%>">
                <form class="d-flex justify-content-between align-items-center bg-light" method="POST" action="/<%= docs[i]._id%>?_method=DELETE">
                    <div class="d-flex flex-column">
                        <h3><a class="text-decoration-none text-info" href="/<%= docs[i].name%>">Name: <%=docs[i].name%></a></h3>
                        <div class="sn">Social Network: <%=docs[i].sn%></div>
                        <div class="text-secondary field url">URL: <%=docs[i].url%></div>
                        <div class="text-danger h4"><i class="material-icons">mouse</i> Clicks: <%=docs[i].clicks%></div>
                        <input type="text" name="<%= docs[i]._name%>" hidden>
                    </div>
                    <div class="d-flex flex-column align-items-center px-5">
                        <a class="btn btn-primary mb-2" href="/edit/<%= docs[i]._id%>">Edit</a>
                        <button class="btn btn-danger" type="submit">Delete</button>
                    </div>
                </form>
            </div>
        <% } %> 

        </div>
</body>
</html>